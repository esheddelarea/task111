properties([parameters([booleanParam(description: 'ESHED ESHED', name: 'NOA NOA')])])
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git 'https://github.com/esheddelarea/task111.git'
                sh "ls -ltr"
                sh "echo noa"
            }
        }
    }
}
